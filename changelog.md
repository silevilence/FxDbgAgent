# Change Log

## V0.1.0

### ✨ 新功能

- 通过标准 MCP（stdio）连接 AI 客户端，提供 15 个调试工具：启动/附加目标进程、源码断点（支持命中次数与只读条件表达式）、继续/暂停/三种单步、线程与调用栈、参数与局部变量分页、受限只读表达式求值、first-chance 异常类型过滤、状态查询与安全分离。
- 自动识别并调试 x86/x64 的 .NET Framework 4.x 桌面应用（含 AnyCPU 32BitPreferred），可附加正在运行的 Windows 服务与完整 IIS 的 ASP.NET 进程，覆盖跨身份权限、影子复制符号定位与应用池回收后重新附加。
- 基于 Windows PDB 的源码断点：延迟加载模块或 PDB 就绪后自动重绑定，断点位置移动、符号缺失/不匹配/读取失败均返回明确状态。
- 只读观察纪律：不调用目标属性 Getter、ToString 或任意函数，不修改目标状态；未处理异常按原始字段报告；日志默认脱敏，可完全关闭。
- 变量读取带分页与深度/成员数/字符串长度边界，循环引用返回标识而不递归展开，恢复运行后旧帧与引用失效并重新获取。
- 会话级源码路径映射（构建机路径↔本地源码），多 AppDomain 识别、筛选与域级断点。
- 可选的 DAP 调试适配器与 VS Code 扩展：源码断点、暂停/继续/单步、线程/栈/变量分页、受限只读求值与安全分离。
- 命令行工具作为开发入口，与 MCP 共享同一调试引擎，命令与工具语义一致。
- 安全生命周期：正常退出或断开时尝试安全分离并让目标继续运行，附加目标禁止被终止，引擎崩溃被隔离不影响主进程。
- 随包提供 AI 技能与安装指南，可脱离源码在无源码机器上完成配置、安装与验证。
